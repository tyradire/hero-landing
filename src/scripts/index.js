document.addEventListener('DOMContentLoaded', () => {
  console.log('Скрипт загружен')
  const flags = document.querySelectorAll('.animate-side__flag');
  console.log(flags);
  // setTimeout(() => {
  //   flags.forEach(elem => elem.classList.add('animate-side__flag_active'))
  // }, 4600);
});